The HUUB challenges is consists of following technology :react redux bootstrap.

Pagination and localstorage is also used and tried to handle as much edge cases as possible.

Commands to start the project::

```
npm install
npm start
```